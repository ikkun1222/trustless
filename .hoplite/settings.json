{
  "version": 1,
  "ports": {"preview": 3000, "additional": {}},
  "scripts": {
    "setup": {
      "enabled": true,
      "command": "mise use -g go@1.26.6 >/dev/null 2>&1; if ! command -v sqlite3 >/dev/null 2>&1; then if command -v sudo >/dev/null 2>&1; then sudo apt-get update -qq && sudo apt-get install -y -qq sqlite3; else apt-get update -qq && apt-get install -y -qq sqlite3; fi; fi"
    },
    "run": {"enabled": true, "command": null},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": null}
  },
  "mcpServers": []
}
